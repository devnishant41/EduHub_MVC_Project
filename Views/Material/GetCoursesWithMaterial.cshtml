@* @model Test_EduHub.Models.Combined.CourseMaterialViewModel

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Course Details</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
    <div class="container mt-5">
        <h2>Course Details</h2>
        <div class="card mb-4">
            <div class="card-body">
                <h4 class="card-title">@Model.Course.Title</h4>
                <p class="card-text"><strong>Description:</strong> @Model.Course.Description</p>
                <p class="card-text"><strong>Start Date:</strong> @Model.Course.CourseStartDate.ToShortDateString()</p>
                <p class="card-text"><strong>End Date:</strong> @Model.Course.CourseEndDate.ToShortDateString()</p>
                <p class="card-text"><strong>Level:</strong> @Model.Course.Level</p>
                <p class="card-text"><strong>Price:</strong> @Model.Course.Price.ToString("C")</p>
            </div>
        </div>

        <h3>Materials</h3>
        <ul class="list-group">
            @foreach (var material in Model.Materials)
            {
                <li class="list-group-item">
                    <h5>@material.Title</h5>
                    <p>@material.Description</p>
                    <a href="@material.Url" target="_blank">View Material</a>
                </li>
            }
        </ul>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html> *@